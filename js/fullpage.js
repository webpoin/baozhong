
	// mui.plusReady(function() {
	// 	// 关闭等待窗
	// 	plus.nativeUI.closeWaiting();
	// 	plus.webview.currentWebview().show('pop-in',300);
	// });

	// // 返回
	// mui.back = function() {
	// 	plus.webview.currentWebview().hide('slide-out-right',300);
	// 	// var home = plus.webview.getLaunchWebview();
	// 	// home && mui.fire(home,'closeFull',{href:location.href});
	// };
	// mui('body').on('tap', '.toback', function(e) {mui.back();});

	
	// // 链接点击
	// mui('body').on('tap','a',function(){

	// 	var href = this.getAttribute('href');
	// 	if(!href){return;}

	// 	var home = plus.webview.getLaunchWebview();
	// 	home && mui.fire(home, 'openFull', {href: this.getAttribute('href')});

	// 	// 显示等	待
	// 	// plus.nativeUI.showWaiting('正在加载...');
	// 	return false;
	// });

